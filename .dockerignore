var/cache/
var/log/
.git*
node_modules/
vendor/
public/build/
docker-*
Dockerfile*
# .env holds the committed defaults the application needs to boot and is part
# of every release; only the local overrides must stay out of build contexts.
.env.local
.env.*.local
